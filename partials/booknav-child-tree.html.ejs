<ul class="booknav-tree"><% for (var i in tree) {
    if (tree[i].type === 'doc') {
        %><li>
            <a href="<%= urlForDoc(tree[i]) %>"><%= tree[i].title %></a>
            <% if (tree[i].teaser) { %><%= tree[i].teaser %><% } %>
        </li><%
    } else {
        %><li>
            <%
            var url = urlForDir(tree[i]);
            if (url !== "undefined") {
                %><a href="<%= url %>"><%= nameForDir(tree[i]) %></a><%
                if (tree[i].teaser) { %><%= tree[i].teaser %><% }
            } %>
            <%- renderSubTree(tree[i]) %>
        </li><%
    } %>
<% } %></ul>
