{
  "name": "akashacms-booknav-test",
  "version": "1.0.0",
  "description": "Test suite for akashacms-booknav module",
  "main": "index.mjs",
  "scripts": {
    "test": "mocha ./index",
    "setup": "npm install && cd .. && npm install && npm install ../akasharender --no-save",
    "clean": "rm -rf node_modules out"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chai": "^4.3.x",
    "mocha": "^9.1.x"
  }
}
